version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: qa1k-vane_vane_1
      APP_PORT: 3000

  vane:
    image: itzcrazykns1337/vane:latest
    restart: unless-stopped
    init: true
    volumes:
      - ${APP_DATA_DIR}/data:/home/vane/data
